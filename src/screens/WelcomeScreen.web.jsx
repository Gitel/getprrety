// Web behaves identically to native here — the only web-specific concern (reading the
// ?ref= query param) is handled in App.js and welcomeVariants.js. Kept as a sibling file
// to match the screen naming convention.
export { default } from './WelcomeScreen';
